#include <stdio.h>

int main()
{
    int n=3;
    int A[n][n];
    for (int i=0;i<n;i++)
        for (int j=0;j<n;j++)
        {
            printf("/nInput value %i, %j", i, j);
            scanf("%i", A[i][j]);
            }
    return 0;
}
